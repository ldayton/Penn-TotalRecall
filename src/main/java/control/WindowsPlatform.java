package control;

/** Windows-specific platform implementation. */
public class WindowsPlatform extends PCPlatform {
    // Inherits all behavior from PCPlatform
    // Override specific methods here if Windows needs different behavior
}
