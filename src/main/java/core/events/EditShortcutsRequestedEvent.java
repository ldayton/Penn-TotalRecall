package core.events;

/**
 * Event published when the user requests to edit keyboard shortcuts. The UI layer handles this by
 * showing the shortcut editor dialog.
 */
public record EditShortcutsRequestedEvent() {}
