package core.viewport;

/** Centralized defaults for viewport behavior. */
public final class ViewportDefaults {
    private ViewportDefaults() {}

    /** Single source of truth for default zoom. */
    public static final int DEFAULT_PIXELS_PER_SECOND = 200;
}
