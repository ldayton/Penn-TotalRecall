<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Default to INFO level -->
    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>
    
    <!-- Set specific loggers if needed -->
    <logger name="control.Start" level="${logging.level:-INFO}" />
    <logger name="audio" level="${logging.level:-INFO}" />
    <logger name="components" level="${logging.level:-INFO}" />
</configuration>
